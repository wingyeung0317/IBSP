# ============================================================================
# Raspberry Pi Gateway - Environment Configuration Template
# ============================================================================
# Copy this file to .env in the same directory as uart_lora_receiver.py
# Source it before running: source .env && python3 uart_lora_receiver.py

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# Backend server IP address (where Docker containers are running)
export SERVER_IP=YOUR_SERVER_IP

# Backend server port (default: 5000)
export SERVER_PORT=5000

# ============================================================================
# DEVICE CONFIGURATION
# ============================================================================
# Gateway device identifier
export DEVICE_ID=Gateway-001

# ============================================================================
# EXAMPLE USAGE
# ============================================================================
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env and replace YOUR_SERVER_IP with actual IP
#
# 3. Source and run:
#    source .env && python3 uart_lora_receiver.py
#
# Or add to systemd service file:
#    EnvironmentFile=/home/pi/raspberry-pi/.env
